<h1>Listing remarks</h1>

<table>
  <thead>
    <tr>
      <th>Bookmark</th>
      <th>Body</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @remarks.each do |remark| %>
      <tr>
        <td><%= remark.bookmark_id %></td>
        <td><%= remark.body %></td>
        <td><%= remark.user %></td>
        <td><%= link_to 'Show', remark %></td>
        <td><%= link_to 'Edit', edit_remark_path(remark) %></td>
        <td><%= link_to 'Destroy', remark, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Remark', new_remark_path %>
